package com.cnc.my.spring.spring_aop;

import org.aspectj.lang.annotation.Aspect;

@Aspect
public class NotVeryUsefulAspect {
    /**
     使用@Aspect 注解标记的类称只为方面, 方面可以认为是容器
     方面可以有方法和字段，与任何其他类相同。
     方面可以包含切入点、建议和介绍（类型间）声明
     */
}
